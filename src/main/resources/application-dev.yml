server:
  port: 8094
  servlet:
    context-path: /stock
spring:
  application:
    name: InventoryService
  datasource:

    url: jdbc:postgresql://inventory.postgres.database.azure.com:5432/Inventory-Stock?CreateDatabaseIfNotExist=true
    username: adminroot
    password: password@123

    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        namingStrategy: org.hibernate.cfg.PhysicalNamingStrategy
  cloud:
    discovery:
      enabled: false
      
swagger:
  url: https://stock-service.ros.skillassure.com/stock
  
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://service-registry.test.restaurantonesolution.com/eureka
  instance:
    hostname: localhost
logging:
  level:
    '[com.ros.inventory]': DEBUG
